name: suraksha_plus
description: A production-grade safety application.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management
  provider: ^6.1.1

  # AWS Amplify
  amplify_flutter: ^1.6.0
  amplify_auth_cognito: ^1.6.0
  amplify_api: ^1.6.0

  # Maps & Location
  google_maps_flutter: ^2.5.0
  geolocator: ^10.1.0
  location: ^5.0.3

  # Bluetooth
  flutter_blue_plus: ^1.20.0
  permission_handler: ^11.1.0

  # Phone / External Intents
  url_launcher: ^6.2.5   # ✅ ADDED (FIXES YOUR ERROR)

  # UI/UX
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.9
  lottie: ^2.7.0

  # Utils
  shared_preferences: ^2.2.2
  uuid: ^4.2.1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/
